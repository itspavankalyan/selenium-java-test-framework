# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
release.properties
dependency-reduced-pom.xml

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store

# Logs and runtime artifacts
logs/
*.log

# Local H2 database files (regenerated by DB validation layer on each run)
*.mv.db
*.trace.db

# Local environment overrides — never commit secrets/credentials
.env
config-local.properties
